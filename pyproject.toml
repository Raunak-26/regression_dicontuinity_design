[build-system]
requires = ["setuptools>=45", "wheel", "setuptools_scm[toml]>=6.0"]


[tool.setuptools_scm]
write_to = "src/rdd_rm/_version.py"

[tool.pytask.ini_options]
paths = ["./src/rdd_rm", "./paper"]
infer_latex_dependencies = true
